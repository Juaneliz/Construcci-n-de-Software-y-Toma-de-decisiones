<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= registro ? 'Registrarse' : 'Iniciar sesión' %></title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 420px; margin: 3rem auto; }
        label { display: block; margin-top: 0.8rem; }
        input[type=text], input[type=password] { width: 100%; padding: 0.5rem; box-sizing: border-box; }
        button { margin-top: 1rem; padding: 0.6rem 1.2rem; }
    </style>
</head>
<body>
    <h2><%= registro ? '¡Únete al lado oscuro, tenemos galletas!' : 'Bienvenido' %></h2>
    <h3>Preguntas de reflexion: Formas de autentificacion existentes  </h3>
        <ul>
            <li><strong>JWT:</strong> Token firmado que el cliente guarda.</li>
            <li><strong>OAuth 2.0:</strong> Login con Google, GitHub, Facebook, etc.</li>
            <li><strong>API Keys:</strong> Clave única que se envía en cada request.</li>
            <li><strong>2FA:</strong> Código temporal vía SMS o Google Authenticator.</li>
            <li><strong>Biometría:</strong> Huella digital o reconocimiento facial.</li>
            <li><strong>Magic Links:</strong> Enlace de un solo uso enviado al email.</li>
        </ul>

    <form action="/usuarios/<%= registro ? 'registro' : 'login' %>" method="POST">
        <label for="username">Nombre de usuario</label>
        <input type="text" id="username" name="username" required>

        <% if (registro) { %>
            <label for="name">Nombre completo</label>
            <input type="text" id="name" name="name">
        <% } %>

        <label for="password">Contraseña</label>
        <input type="password" id="password" name="password" required>

        <button type="submit"><%= registro ? '¡Unirme!' : 'Entrar' %></button>
    </form>
</body>
</html>