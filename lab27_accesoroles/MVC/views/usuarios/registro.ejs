<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= registro ? 'Registrarse' : 'Iniciar sesión' %></title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 420px; margin: 3rem auto; }
        label { display: block; margin-top: 0.8rem; }
        input[type=text], input[type=password] { width: 100%; padding: 0.5rem; box-sizing: border-box; }
        button { margin-top: 1rem; padding: 0.6rem 1.2rem; }
    </style>
</head>
<body>
    <h2><%= registro ? '¡Únete al lado oscuro, tenemos galletas!' : 'Bienvenido' %></h2>

    <form action="/usuarios/<%= registro ? 'registro' : 'login' %>" method="POST">
        <label for="username">Nombre de usuario</label>
        <input type="text" id="username" name="username" required>

        <% if (registro) { %>
            <label for="name">Nombre completo</label>
            <input type="text" id="name" name="name">
        <% } %>

        <label for="password">Contraseña</label>
        <input type="password" id="password" name="password" required>

        <button type="submit"><%= registro ? '¡Unirme!' : 'Entrar' %></button>
    </form>
    <h2>En que consiste el control de acceso basado en roles</h2>
        <p>Modelo de seguridad donde se asignan permisos
            Por medio de roles. Cada usuario maneja un rol, por lo tanto
            no encontramos mismas paginas por usuario.
        </p>
    <h2>Sistemas con y sin RBAC</h2>
        <p>Sistemas con y Sin RBAC</p>
        <p>Con: El sistema del proyecto</p>
        <li>Al usar RBAC permitimos que exista una diferencia entre usuarios</li>
        <p>Sin:Paginas tradicionales</p>
        <li>Permisos se asignan a cada usuario por archivo.</li>
</body>
</html>